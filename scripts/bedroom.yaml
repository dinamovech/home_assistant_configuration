# https://www.home-assistant.io/docs/scripts/

# Включение стационарного компьютера + ИБП
turnon_desktop_pc_and_ups:
  alias: "Включить ИБП + компьютер"
  icon: mdi:desktop-classic
  sequence:
    #- service: switch.turn_on
    #  entity_id: switch.pc_ups
    #- delay: "00:00:01"
    - service: switch.turn_on
      entity_id: switch.desktop_pc

# Выключение стационарного компьютера + ИБП
turnoff_desktop_pc_and_ups:
  alias: "Отключить ИБП + компьютер"
  icon: mdi:desktop-classic
  sequence:
    - service: switch.turn_off
      entity_id: switch.desktop_pc
    #- wait_template: "{{ is_state('switch.desktop_pc', 'off') }}"
    #- delay: "00:00:10"
    #- service: switch.turn_off
    #  entity_id: switch.pc_ups
